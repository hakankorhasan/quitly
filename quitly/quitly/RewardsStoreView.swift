//
//  RewardsStoreView.swift
//  quitly
//
//  Stub — Rewards Store feature removed from Quit Masturbation app.
//

import SwiftUI

// Empty stub — kept to prevent build errors from stale references
struct RewardsStoreView: View {
    let habit: Habit
    var body: some View { EmptyView() }
}
