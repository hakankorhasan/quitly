//
//  NextRewardProgressView.swift
//  quitly
//
//  Stub — Rewards Store feature removed.
//

import SwiftUI

// Empty stub — kept to prevent build errors from stale references
struct NextRewardProgressView: View {
    let habit: Habit
    var onTap: (() -> Void)? = nil
    var body: some View { EmptyView() }
}
